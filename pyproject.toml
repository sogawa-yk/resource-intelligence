[project]
name = "ri-agent"
version = "0.1.0"
description = "OCI Resource Intelligence Agent"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
target-version = "py311"
src = ["src", "tests"]
